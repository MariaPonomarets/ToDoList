import {StyleSheet} from 'react-native';

import {primaryInput, sizeXL} from '../../components/typography';

export default StyleSheet.create({
  title: {
    fontSize: sizeXL,
    color: primaryInput,
    margin: 20,
  },
  button: {
    margin: 20,
  },
});
